services:
  shell:
    build: ./mf-shell
    ports:
      - "3000:3000"
    volumes:
      - ./mf-shell:/app
    command: npm run dev -- --host 0.0.0.0
    depends_on:
      - character-list
      - character-detail

  character-list:
    build: ./mf-characters
    ports:
      - "3001:3001"
    volumes:
      - ./mf-characters:/app
    command: npm run dev -- --host 0.0.0.0

  character-detail:
    build: ./mf-character-detail
    ports:
      - "3002:3002"
    volumes:
      - ./mf-character-detail:/app
    command: npm run dev -- --host 0.0.0.0
